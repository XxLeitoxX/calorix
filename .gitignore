# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
.env
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Accidental files created by running `eval "$(ssh-agent -s)"`
# (contain shell output / key material, never should be committed)
eval "$(ssh-agent -s)"
eval "$(ssh-agent -s)".pub
